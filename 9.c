#include <stdio.h>
void main()
{
    int sum=0
    for(int i=0;i<rows;i++)
    {
        for(int j=0;j<cols,j++)
        {
            sum=sum+Array[i][j];
        }
        printf("\n sum of all the elements:",sum)
    }
}
